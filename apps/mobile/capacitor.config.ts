import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'me.alikuxac.aligrid.mobile',
    appName: 'AliGrid',
    webDir: 'dist'
};

export default config;